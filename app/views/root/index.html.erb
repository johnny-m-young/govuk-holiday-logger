<%= render "govuk_publishing_components/components/title", {
  title: "#{current_user.given_name} #{current_user.family_name}'s Dashboard",
  average_title_length: "long",
  margin_top: 0
} %>

<%= render "govuk_publishing_components/components/panel", {
  prepend: "Annual leave balance",
  title: "#{current_user.annual_leave_remaining} days"
} %>

<%= render "govuk_publishing_components/components/button", {
  text: "Request annual leave",
  href: new_annual_leave_request_path,
  start: true,
  rel: "external"
} %>

<div class="govuk-!-margin-top-6">
  <% @annual_leave_requests.each do |annual_leave_request| %>
    <%= render "shared/annual_leave_request_card", card_links: "root/edit_or_withdraw_links", annual_leave_request: %>
  <% end %>
<div>

